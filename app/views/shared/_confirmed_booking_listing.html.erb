
 <% @confirmed.each do |request| %>
          <div class="booking-listing">
            <div class="row">
              <div class="col-xs-12 col-md-2">
                <%= cl_image_tag Consumer.find_by(user_id: request.user_id).profile_img, class: 'avatar-large hidden-xs', width: 300, height: 300, crop: :fill %>
              </div>
              <div class="col-xs-8 col-md-7">
                <%= link_to artist_consumers_request_path(request.artist_id) do %>
                  <p class="booking-date"><%= request.date.strftime('%b %d %Y, %I:%M%p') %></p>
                <% end %>
                <p class="booking-time"><%= request.time %></p>
                <p class="booking-category"><%= request.servicename%></p>
                <p class="booking-address"><%= request.address%></p>
              </div>
              <div class="col-xs-4 col-md-3">
                <div class="artist-quote-price">
                  <p class="booking-price">$<%= request.final_price %></p>
                  <p class="status-confirmed"><%= request.status %></p>

                </div>
              </div>
            </div>
          </div>
  <% end %>
