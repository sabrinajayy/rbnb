      <%=  simple_form_for(@consumer_request, :url => artist_consumers_requests_path, :method => :post) do  |f| %>
      <%= f.input :base_price %>
      <%= f.input :fees %>
      <%= f.input :final_price %>
      <%= f.submit 'Request this artist!', :url => new_artist_consumers_request_path(@artist.id), class: 'btn btn-primary' %>
      <%#= button_to "Request this artist!", new_artist_consumers_request_path(artist.id),
      :class => "btn-pamprd", :method => :get %>
      <% end %>
